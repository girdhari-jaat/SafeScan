package com.safescan.android.ml.local

import android.graphics.Bitmap
import com.safescan.android.scanner.Quadrilateral

class LocalMLEngine {
    fun detectCorners(bitmap: Bitmap): Quadrilateral? {
        // Existing local ML edge detection logic preserved here as requested
        return null
    }
}
